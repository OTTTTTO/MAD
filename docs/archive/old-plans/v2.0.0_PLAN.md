# MAD v2.0.0 开发计划

**版本：** v2.0.0
**主题：** 下一代智能讨论平台
**优先级：** P0（战略级）
**预计时间：** 2-3 小时

---

## 🎯 核心功能

### 1. 多模态输入支持
**用户价值：** 支持图片、文件、代码等多模态输入
**技术难度：** ⭐⭐⭐⭐

**功能描述：**
- 上传图片到讨论
- 支持 PDF、Word 文档
- 代码片段高亮
- 语音输入（未来）
- 视频分析（未来）

**技术实现：**
```javascript
// 多模态内容处理
class MultimodalContent {
  constructor() {
    this.supportedTypes = [
      'text/plain',
      'image/jpeg',
      'image/png',
      'application/pdf',
      'application/msword'
    ];
  }
  
  async processContent(content) {
    const type = this.detectType(content);
    
    switch (type) {
      case 'image':
        return await this.processImage(content);
      case 'pdf':
        return await this.processPDF(content);
      case 'code':
        return await this.processCode(content);
      default:
        return content;
    }
  }
  
  async processImage(imageData) {
    // 使用视觉模型提取图片内容
    const description = await this.describeImage(imageData);
    return {
      type: 'image',
      data: imageData,
      description,
      timestamp: Date.now()
    };
  }
  
  async processPDF(pdfData) {
    // 提取 PDF 文本
    const text = await this.extractPDFText(pdfData);
    return {
      type: 'pdf',
      data: pdfData,
      text,
      pageCount: await this.getPDFPageCount(pdfData),
      timestamp: Date.now()
    };
  }
}
```

**UI 设计：**
```
┌─────────────────────────────────┐
│ 💬 消息输入                     │
│ ┌─────────────────────────────┐ │
│ │                              │ │
│ └─────────────────────────────┘ │
│ [📎] [🖼️] [📄] [💻] [🎤] [发送]│
└─────────────────────────────────┘

上传图片后：
┌─────────────────────────────────┐
│ 💡 [主协调员]                    │
│ 📷 [图片预览]                   │
│ 🤖 视觉分析：这是一个产品原型... │
│                                 │
│ [🖼️ 查看大图] [🔍 分析详情]     │
└─────────────────────────────────┘
```

---

### 2. Agent 自定义与市场
**用户价值：** 用户可以创建和分享自己的 Agent
**技术难度：** ⭐⭐⭐⭐⭐

**功能描述：**
- Agent 可视化编辑器
- 自定义 Prompt 模板
- Agent 市场（分享/下载）
- Agent 评分和评论
- Agent 版本管理

**技术实现：**
```javascript
// Agent 定义格式
const agentDefinition = {
  id: 'custom_agent_001',
  name: '法律顾问',
  emoji: '⚖️',
  description: '专注于法律合规性分析',
  systemPrompt: `你是一位专业的法律顾问...`,
  expertise: ['法律', '合规', '合同'],
  personality: {
    formality: 'high',
    verbosity: 'medium',
    creativity: 'low'
  },
  capabilities: [
    '法律分析',
    '合规检查',
    '风险评估'
  ],
  examples: [
    {
      input: '这个功能是否合规？',
      response: '从法律角度来看...'
    }
  ],
  version: '1.0.0',
  author: 'user123',
  rating: 4.5,
  downloads: 1234
};

// Agent 编辑器
class AgentEditor {
  constructor() {
    this.agent = null;
    this.previewMode = false;
  }
  
  createNew() {
    this.agent = {
      id: generateId(),
      name: '新 Agent',
      emoji: '🤖',
      systemPrompt: '',
      expertise: [],
      personality: {}
    };
  }
  
  editField(field, value) {
    this.agent[field] = value;
    this.updatePreview();
  }
  
  async testAgent() {
    // 测试 Agent 响应
    const testMessage = '你好，请介绍一下自己';
    const response = await this.callAgent(this.agent, testMessage);
    return response;
  }
  
  async publish() {
    // 发布到市场
    await marketAPI.publish(this.agent);
  }
}
```

**UI 设计：**
```
┌─────────────────────────────────┐
│ 🤖 Agent 编辑器                 │
│ ┌─────────────────────────────┐ │
│ │ 名称: [法律顾问    ]        │ │
│ │ Emoji: [⚖️]                  │ │
│ │ 描述: [法律合规分析...]      │ │
│ └─────────────────────────────┘ │
│ ┌─────────────────────────────┐ │
│ │ 系统提示词:                  │ │
│ │ ┌─────────────────────────┐ │ │
│ │ │ 你是一位专业的法律顾问...│ │ │
│ │ │                         │ │ │
│ │ └─────────────────────────┘ │ │
│ └─────────────────────────────┘ │
│ ┌─────────────────────────────┐ │
│ │ 专业领域: [法律] [合规] [+] │ │
│ │ 性格设置:                    │ │
│ │ 正式度: ████████░ 80%       │ │
│ │ 话密度: ██████░░░ 60%       │ │
│ │ 创造性: ███░░░░░░ 30%       │ │
│ └─────────────────────────────┘ │
│ [测试] [预览] [保存] [发布]     │
└─────────────────────────────────┘

Agent 市场：
┌─────────────────────────────────┐
│ 🏪 Agent 市场                   │
│ ┌─────────────────────────────┐ │
│ │ 🔥 热门                     │ │
│ │ ⚖️ 法律顾问 (⭐4.5) 📥1.2k  │ │
│ │ 📈 数据分析师 (⭐4.8) 📥2.3k │ │
│ │ 🎨 UI设计师 (⭐4.2) 📥987   │ │
│ └─────────────────────────────┘ │
│ ┌─────────────────────────────┐ │
│ │ ✨ 最新                     │ │
│ │ 🧪 质量工程师 (⭐4.0) 📥156 │ │
│ │ 🔒 安全专家 (⭐4.6) 📥234   │ │
│ └─────────────────────────────┘ │
└─────────────────────────────────┘
```

---

### 3. 讨论分支与并行
**用户价值：** 支持复杂话题的分支讨论
**技术难度：** ⭐⭐⭐⭐

**功能描述：**
- 从主讨论创建分支
- 并行讨论多个子话题
- 分支合并机制
- 分支可视化（树状图）
- 跨分支引用

**技术实现：**
```javascript
// 讨论分支结构
class DiscussionBranch {
  constructor(parentId, topic) {
    this.id = generateId();
    this.parentId = parentId;
    this.topic = topic;
    this.messages = [];
    this.children = [];
    this.status = 'active';
    this.createdAt = Date.now();
  }
  
  createChild(topic) {
    const branch = new DiscussionBranch(this.id, topic);
    this.children.push(branch);
    return branch;
  }
  
  mergeIntoParent() {
    const parent = getDiscussion(this.parentId);
    if (!parent) return false;
    
    // 合并消息到父讨论
    this.messages.forEach(msg => {
      parent.addMessage(msg.role, msg.content, {
        ...msg.metadata,
        fromBranch: this.id,
        branchTopic: this.topic
      });
    });
    
    this.status = 'merged';
    return true;
  }
  
  getTree() {
    return {
      id: this.id,
      topic: this.topic,
      status: this.status,
      messageCount: this.messages.length,
      children: this.children.map(child => child.getTree())
    };
  }
}

// 分支可视化
function renderBranchTree(discussion) {
  const tree = discussion.getTree();
  
  return `
    <div class="branch-tree">
      ${renderBranchNode(tree)}
    </div>
  `;
}

function renderBranchNode(node, level = 0) {
  const indent = level * 20;
  
  return `
    <div class="branch-node" style="margin-left: ${indent}px;">
      <div class="branch-header">
        <span class="branch-status">${node.status}</span>
        <span class="branch-topic">${node.topic}</span>
        <span class="branch-count">(${node.messageCount})</span>
      </div>
      ${node.children.map(child => renderBranchNode(child, level + 1)).join('')}
    </div>
  `;
}
```

**UI 设计：**
```
┌─────────────────────────────────┐
│ 🌳 讨论分支                     │
│ ┌─────────────────────────────┐ │
│ │ 📋 主讨论: 产品功能规划     │ │
│ │    💬 25 条消息             │ │
│ │    [创建分支]               │ │
│ │    ├─ 🔧 技术实现 (10)     │ │
│ │    │  ├─ 📱 前端方案 (5)   │ │
│ │    │  └─ 🗄️ 后端方案 (5)   │ │
│ │    ├─ 🎨 UI设计 (8)        │ │
│ │    └─ 📊 市场分析 (7)      │ │
│ └─────────────────────────────┘ │
│                                 │
│ [合并分支] [查看树状图]         │
└─────────────────────────────────┘
```

---

### 4. 讨论回顾与智能总结
**用户价值：** 自动生成讨论回顾和行动计划
**技术难度：** ⭐⭐⭐⭐⭐

**功能描述：**
- AI 自动生成讨论总结
- 提取关键决策点
- 生成思维导图
- 创建行动时间线
- 发送回顾邮件

**技术实现：**
```javascript
// 智能总结生成
class DiscussionSummarizer {
  async summarize(discussion) {
    const summary = {
      overview: await this.generateOverview(discussion),
      keyPoints: await this.extractKeyPoints(discussion),
      decisions: await this.extractDecisions(discussion),
      actionItems: await this.extractActionItems(discussion),
      timeline: await this.generateTimeline(discussion),
      mindMap: await this.generateMindMap(discussion)
    };
    
    return summary;
  }
  
  async generateOverview(discussion) {
    // 使用 LLM 生成总体概述
    const prompt = `
      讨论主题: ${discussion.topic}
      消息数: ${discussion.messages.length}
      参与者: ${discussion.participants.map(p => p.role).join(', ')}
      
      请生成一个简短的概述（2-3 句话）。
    `;
    
    return await this.callLLM(prompt);
  }
  
  async extractKeyPoints(discussion) {
    // 提取关键观点
    const keyPoints = [];
    
    discussion.messages.forEach(msg => {
      const points = this.extractPoints(msg.content);
      keyPoints.push(...points);
    });
    
    // 去重和排序
    return this.deduplicateAndRank(keyPoints).slice(0, 5);
  }
  
  async generateMindMap(discussion) {
    // 生成思维导图结构
    const nodes = [
      { id: 'root', label: discussion.topic, level: 0 }
    ];
    
    const keyPoints = await this.extractKeyPoints(discussion);
    keyPoints.forEach((point, index) => {
      nodes.push({
        id: `point-${index}`,
        label: point.text,
        level: 1,
        parent: 'root'
      });
      
      // 添加子观点
      point.subPoints.forEach((subPoint, subIndex) => {
        nodes.push({
          id: `subpoint-${index}-${subIndex}`,
          label: subPoint,
          level: 2,
          parent: `point-${index}`
        });
      });
    });
    
    return nodes;
  }
}
```

**UI 设计：**
```
┌─────────────────────────────────┐
│ 📊 讨论回顾                     │
│ ┌─────────────────────────────┐ │
│ │ 📝 概述                     │ │
│ │ 本次讨论聚焦于新功能的...    │ │
│ └─────────────────────────────┘ │
│ ┌─────────────────────────────┐ │
│ │ 💡 关键观点                 │ │
│ │ 1. 采用微服务架构           │ │
│ │ 2. 优先实现核心功能         │ │
│ │ 3. 需要充分的测试           │ │
│ └─────────────────────────────┘ │
│ ┌─────────────────────────────┐ │
│ │ ✅ 决策                     │ │
│ │ • 使用 React + Node.js      │ │
│ │ • 采用敏捷开发流程           │ │
│ │ • 预计 6 周完成             │ │
│ └─────────────────────────────┘ │
│ ┌─────────────────────────────┐ │
│ │ 📅 时间线                   │ │
│ │ Week 1-2: 需求分析          │ │
│ │ Week 3-4: 开发              │ │
│ │ Week 5-6: 测试和部署        │ │
│ └─────────────────────────────┘ │
│ [🧠 思维导图] [📧 发送邮件]    │
└─────────────────────────────────┘
```

---

## 📋 开发步骤

### Step 1: 多模态输入支持（45 分钟）
1. 实现文件上传功能
2. 图片处理和显示
3. PDF 文本提取
4. 代码高亮支持
5. 测试各种文件类型

### Step 2: Agent 自定义与市场（60 分钟）
1. Agent 编辑器 UI
2. Agent 定义格式
3. Agent 市场 API
4. 评分和评论系统
5. 测试创建和分享

### Step 3: 讨论分支与并行（45 分钟）
1. 分支数据结构
2. 创建和管理分支
3. 分支可视化
4. 合并机制
5. 测试分支流程

### Step 4: 讨论回顾与智能总结（60 分钟）
1. 总结算法实现
2. 关键点提取
3. 思维导图生成
4. 时间线可视化
5. 测试总结质量

### Step 5: 集成测试与优化（30 分钟）
1. 端到端测试
2. 性能优化
3. 用户体验测试
4. 文档完善

---

## 🎯 验收标准

### 多模态输入
- ✅ 支持图片上传和显示
- ✅ 支持 PDF 文本提取
- ✅ 代码高亮正常
- ✅ 文件大小限制合理

### Agent 自定义
- ✅ 可以创建自定义 Agent
- ✅ Agent 编辑器易用
- ✅ 市场功能正常
- ✅ 评分系统工作

### 讨论分支
- ✅ 可以创建分支
- ✅ 分支可视化清晰
- ✅ 合并功能正常
- ✅ 跨分支引用支持

### 智能总结
- ✅ 总结准确率 > 80%
- ✅ 关键点提取准确
- ✅ 思维导图可读
- ✅ 时间线合理

---

## 📊 成功指标

- **多模态使用率：** 40%+ 的讨论包含非文本内容
- **自定义 Agent：** 创建 100+ 个自定义 Agent
- **分支使用率：** 25%+ 的讨论使用分支
- **总结满意度：** 85%+ 的用户认为总结有用

---

## 🚀 开始开发

**开始时间：** 2026-02-02 11:00
**预计完成：** 2026-02-02 14:00

**优先级：** 🔥🔥🔥🔥🔥

Let's build the future! 🚀
