# MAD FileBased 分支

## 📋 分支说明

这是MAD项目的**实验性分支**，探索基于**共享文件**的多Agent通信方案。

### 核心理念

通过**共享文件**实现Agent和Web界面的完全解耦：

```
协调器Agent（有tool权限）
  ↓ 生成数据
共享文件（JSON）
  ↓ 读取数据
Web界面（无tool权限）
```

### 与主分支的区别

| 特性 | main分支 (v4.1.0) | filebase分支 (v0.0.1) |
|------|-----------------|---------------------|
| **架构** | 单一Web服务器 | Agent + Web分离 |
| **LLM调用** | 无（用户手动输入） | 协调器Agent调用 |
| **通信方式** | 直接内存 | 共享文件 |
| **tool依赖** | ❌ Web无tool | ✅ Agent有tool |
| **解耦程度** | 低 | 高 |

### 目标

1. ✅ **解耦Agent和Web** - Agent专注业务，Web专注展示
2. ✅ **简化部署** - Web无需任何配置即可运行
3. ✅ **提高可靠性** - 文件系统天然持久化
4. ✅ **易于扩展** - 可以添加多个数据消费者

### 版本策略

- **版本号**: 0.0.1-filebase
- **开发模式**: 快速迭代，小步验证
- **合并计划**: 验证成功后合并到main分支

---

## 🚀 开发计划

### 阶段0：分支初始化 ✅
- [x] 创建filebase分支
- [x] 更新版本号
- [x] 创建目录结构
- [x] 编写分支说明

### 阶段1：数据文件系统 ⏳
- [ ] 文件管理器实现
- [ ] 配置管理
- [ ] 工具函数
- [ ] 单元测试

### 阶段2：协调器Agent
- [ ] Agent框架
- [ ] 请求轮询
- [ ] 模拟讨论创建
- [ ] 集成测试

### 阶段3：Web界面
- [ ] 文件读取API
- [ ] 数据展示
- [ ] 请求创建API
- [ ] UI优化

### 阶段4：集成测试
- [ ] 端到端测试
- [ ] 性能测试
- [ ] 文档完善

---

## 📂 目录结构

```
filebase/
├── src/
│   ├── coordinator/       # 协调器Agent
│   │   ├── index.js       # 入口
│   │   ├── agent.js       # Agent逻辑
│   │   └── handler.js     # 请求处理
│   │
│   ├── web/              # Web服务器
│   │   ├── server.js      # HTTP服务
│   │   ├── api.js         # API路由
│   │   └── file-reader.js # 文件读取
│   │
│   └── lib/              # 公共库
│       ├── file-manager.js # 文件管理
│       ├── config.js       # 配置
│       └── utils.js        # 工具函数
│
├── data/                  # 数据目录
│   ├── discussions/       # 讨论数据
│   ├── requests/          # 请求队列
│   └── reports/           # 报告
│
├── test/                  # 测试
│   └── file-manager.test.js
│
└── README.md             # 本文件
```

---

## 🔧 使用方法

### 开发模式

```bash
# 1. 切换到filebase分支
git checkout filebase

# 2. 安装依赖（如果有新增）
npm install

# 3. 测试文件系统
npm test

# 4. 启动协调器Agent（需要OpenClaw环境）
node filebase/src/coordinator/index.js

# 5. 启动Web服务器
node filebase/src/web/server.js
```

### 数据目录

```bash
# 创建数据目录
mkdir -p ~/.openclaw/multi-agent-discuss/{discussions,requests,reports}

# 数据目录结构
~/.openclaw/multi-agent-discuss/
├── discussions/          # 讨论数据
│   └── disc-{id}/
│       ├── discussion.json
│       └── messages.jsonl
├── requests/            # 请求队列
│   ├── pending-{id}.json
│   └── processed/
└── reports/             # 报告
    └── report-{date}.json
```

---

## 📊 当前进度

- **当前阶段**: 阶段0完成
- **完成度**: 10%
- **下一步**: 实现文件管理器

---

## 🤝 贡献

这是一个实验性分支，欢迎：
- 提出改进建议
- 报告Bug
- 提交代码

---

## 📄 许可证

与主分支相同：MIT
